ALTER TABLE public.settings
  ADD COLUMN IF NOT EXISTS work_days TEXT NOT NULL DEFAULT '1,2,3,4,5',
  ADD COLUMN IF NOT EXISTS block_non_work_days BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS late_grace_minutes INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS early_leave_before TIME NOT NULL DEFAULT '15:00:00',
  ADD COLUMN IF NOT EXISTS voice_enabled BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS voice_rate NUMERIC NOT NULL DEFAULT 1.0,
  ADD COLUMN IF NOT EXISTS voice_volume NUMERIC NOT NULL DEFAULT 1.0,
  ADD COLUMN IF NOT EXISTS voice_duplicate_template TEXT NOT NULL DEFAULT 'สแกนซ้ำ {name} บันทึกเวลาไปแล้ว',
  ADD COLUMN IF NOT EXISTS voice_late_suffix TEXT NOT NULL DEFAULT 'มาสายนะคะ',
  ADD COLUMN IF NOT EXISTS voice_denied_text TEXT NOT NULL DEFAULT 'ไม่พบข้อมูล กรุณาติดต่อเจ้าหน้าที่',
  ADD COLUMN IF NOT EXISTS voice_out_of_window_text TEXT NOT NULL DEFAULT 'ยังไม่ถึงเวลาสแกน กรุณาติดต่อเจ้าหน้าที่',
  ADD COLUMN IF NOT EXISTS kiosk_show_recent BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS kiosk_recent_limit INTEGER NOT NULL DEFAULT 20,
  ADD COLUMN IF NOT EXISTS kiosk_mirror BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS kiosk_show_clock BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS kiosk_show_confidence BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS min_face_coverage NUMERIC NOT NULL DEFAULT 0.10,
  ADD COLUMN IF NOT EXISTS detector_min_score NUMERIC NOT NULL DEFAULT 0.5,
  ADD COLUMN IF NOT EXISTS log_unknown_attempts BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS snapshot_retention_days INTEGER NOT NULL DEFAULT 90,
  ADD COLUMN IF NOT EXISTS log_retention_days INTEGER NOT NULL DEFAULT 365,
  ADD COLUMN IF NOT EXISTS default_report_days INTEGER NOT NULL DEFAULT 7;